parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpunit: ~
        phpcs:
            standard: PSR2
            ignore_patterns:
                - test/*
        composer:
            no_check_lock: true
        yamllint:
            ignore_patterns:
                - "#test/(.*).yml#"
        phplint: ~
